<Window x:Class="SpsGui.FindSteamExeServiceTest"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Find Steam Exe Service Test"
        Width="980"
        Height="560"
        MinWidth="820"
        MinHeight="480">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="0,0,8,8" />
            <Setter Property="Padding" Value="12,6" />
            <Setter Property="MinHeight" Value="34" />
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="160" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                BorderBrush="#D0D7DE"
                BorderThickness="1"
                CornerRadius="4"
                Padding="10"
                Margin="0,0,0,10">
            <DockPanel LastChildFill="True">
                <WrapPanel DockPanel.Dock="Right">
                    <Button Content="Find"
                            Click="FindButton_Click" />
                    <Button x:Name="ApplyButton"
                            Content="Use best path"
                            IsEnabled="False"
                            Click="ApplyButton_Click" />
                </WrapPanel>
                <StackPanel>
                    <TextBlock Text="Best path"
                               FontWeight="SemiBold"
                               Margin="0,0,0,4" />
                    <TextBlock x:Name="BestPathTextBlock"
                               Text="-"
                               FontFamily="Consolas"
                               TextWrapping="Wrap" />
                </StackPanel>
            </DockPanel>
        </Border>

        <Border Grid.Row="1"
                BorderBrush="#D0D7DE"
                BorderThickness="1"
                CornerRadius="4"
                Padding="10">
            <DockPanel LastChildFill="True">
                <TextBlock DockPanel.Dock="Top"
                           Text="Candidates"
                           FontWeight="SemiBold"
                           Margin="0,0,0,8" />
                <ListView ItemsSource="{Binding CandidateRows}"
                          ScrollViewer.VerticalScrollBarVisibility="Auto"
                          ScrollViewer.HorizontalScrollBarVisibility="Auto">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Source" Width="220" DisplayMemberBinding="{Binding Source, Mode=OneWay}" />
                            <GridViewColumn Header="PID" Width="80" DisplayMemberBinding="{Binding ProcessIdText, Mode=OneWay}" />
                            <GridViewColumn Header="Path" Width="620" DisplayMemberBinding="{Binding Path, Mode=OneWay}" />
                        </GridView>
                    </ListView.View>
                </ListView>
            </DockPanel>
        </Border>

        <TextBox x:Name="LogTextBox"
                 Grid.Row="2"
                 FontFamily="Consolas"
                 FontSize="12"
                 IsReadOnly="True"
                 AcceptsReturn="True"
                 TextWrapping="NoWrap"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 Margin="0,10,0,0" />
    </Grid>
</Window>
