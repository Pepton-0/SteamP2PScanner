<mah:MetroWindow x:Class="SpsGui.SteamAppIdSelectDialogTest"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
                 Title="Steam AppID"
                 Width="460"
                 Height="230"
                 MinWidth="420"
                 MinHeight="220"
                 WindowStartupLocation="CenterOwner"
                 GlowBrush="#2F6FED">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="WindowTextBlock"
                   Grid.Row="0"
                   FontWeight="SemiBold"
                   TextTrimming="CharacterEllipsis"
                   Margin="0,0,0,12" />

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="96" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="SteamAppId" VerticalAlignment="Center" />
            <TextBox x:Name="SteamAppIdTextBox"
                     Grid.Column="1"
                     FontFamily="Consolas"
                     VerticalContentAlignment="Center"
                     TextChanged="SteamAppIdTextBox_TextChanged" />
        </Grid>

        <TextBlock x:Name="ValidationTextBlock"
                   Grid.Row="2"
                   Foreground="#B42318"
                   Margin="96,8,0,0"
                   VerticalAlignment="Top" />

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Width="92" Margin="0,0,8,0" Click="CancelButton_Click" />
            <Button x:Name="OkButton"
                    Content="OK"
                    Width="92"
                    IsDefault="True"
                    IsEnabled="False"
                    Click="OkButton_Click" />
        </StackPanel>
    </Grid>
</mah:MetroWindow>
