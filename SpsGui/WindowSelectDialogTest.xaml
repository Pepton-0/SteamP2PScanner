<mah:MetroWindow x:Class="SpsGui.WindowSelectDialogTest"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
                 Title="Select Window"
                 Width="840"
                 Height="560"
                 MinWidth="720"
                 MinHeight="460"
                 WindowStartupLocation="CenterOwner"
                 GlowBrush="#2F6FED">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ListView x:Name="WindowsListView"
                  Grid.Row="0"
                  ItemsSource="{Binding Windows}"
                  SelectedItem="{Binding SelectedWindowRow, Mode=TwoWay}"
                  MouseDoubleClick="WindowsListView_MouseDoubleClick"
                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                  ScrollViewer.HorizontalScrollBarVisibility="Auto">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Title" Width="340" DisplayMemberBinding="{Binding Title}" />
                    <GridViewColumn Header="Process" Width="150" DisplayMemberBinding="{Binding ProcessName}" />
                    <GridViewColumn Header="PID" Width="80" DisplayMemberBinding="{Binding ProcessId}" />
                    <GridViewColumn Header="Path" Width="360" DisplayMemberBinding="{Binding ProcessPath}" />
                    <GridViewColumn Header="Thread" Width="80" DisplayMemberBinding="{Binding ThreadId}" />
                    <GridViewColumn Header="HWND" Width="140" DisplayMemberBinding="{Binding HandleText}" />
                </GridView>
            </ListView.View>
        </ListView>

        <DockPanel Grid.Row="1" Margin="0,12,0,0">
            <TextBlock DockPanel.Dock="Left"
                       Text="{Binding SelectionStatus}"
                       Foreground="#57606A"
                       VerticalAlignment="Center" />
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel" Width="92" Margin="0,0,8,0" Click="CancelButton_Click" />
                <Button x:Name="OkButton"
                        Content="OK"
                        Width="92"
                        IsDefault="True"
                        IsEnabled="False"
                        Click="OkButton_Click" />
            </StackPanel>
        </DockPanel>
    </Grid>
</mah:MetroWindow>
